#!/usr/bin/env node

var appPackage = process.env.APP_PACKAGE; 
global.appPackageJson = require(appPackage + '/package.json');
console.log("Starting app : ", appPackage); 
var app = require(appPackage); 
app.start();
